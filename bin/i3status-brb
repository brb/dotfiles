#!/bin/sh

i3status | (read line && echo "$line" && read line && echo "$line" && read line && echo "$line" && while :
do
  read line
  echo -n ",["
  echo -n "{\"full_text\":\"$(xkblayout-state print '%s')\" }," || exit 1
  echo "${line#,\[}" || exit 1
done)
