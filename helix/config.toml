theme = "acme"

[editor]
bufferline = "always"
auto-pairs = false
cursorline = true

[editor.soft-wrap]
enable = true

[editor.file-picker]
hidden = false

[editor.whitespace]
render = "all"

[keys.normal]
C-g = ":sh tmux popup -d \"#{pane_current_path}\" -xC -yC -w80% -h80% -E tig blame TODO"
